# Bibliothèques Python nécessaires pour l'agent de trading
# Installe tout avec : pip install -r requirements.txt

anthropic>=0.40.0        # API Claude (IA Anthropic)
ccxt>=4.2.0              # Connexion aux exchanges crypto (Binance)
requests>=2.31.0         # Appels HTTP vers NewsData.io
python-dotenv>=1.0.0     # Lecture du fichier .env (clés secrètes)
openpyxl>=3.1.2          # Lecture/écriture de fichiers Excel
